const { Row, Typography } = require("antd")

const { Title, Text } = Typography;

const Header = () => {
    return (
        <Row >
            <Title >A Bank</Title>
            
        </Row>
    )
};